server:
  port: 8080

spring:
  application:
    name: currency-parser

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: currency-parser
